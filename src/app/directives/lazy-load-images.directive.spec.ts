import { LazyLoadImagesDirective } from './lazy-load-images.directive';

describe('LazyLoadImageDirective', () => {
  it('should create an instance', () => {
    const directive = LazyLoadImagesDirective;
    expect(directive).toBeTruthy();
  });
});
