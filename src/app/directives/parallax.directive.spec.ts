import { ParallaxDirective } from './parallax.directive';

describe('ParallaxDirective', () => {
  it('should create an instance', () => {
    const directive = ParallaxDirective;
    expect(directive).toBeTruthy();
  });
});
