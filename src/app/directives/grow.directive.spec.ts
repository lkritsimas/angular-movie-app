import { GrowDirective } from './grow.directive';

describe('GrowDirective', () => {
  it('should create an instance', () => {
    const directive = GrowDirective;
    expect(directive).toBeTruthy();
  });
});
